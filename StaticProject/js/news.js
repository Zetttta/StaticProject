function deleteConfirm(){
      	  var check = confirm("削除を進みますか？");
      	  /* if(check == true) else false */
      	  if(check) {
            target
            alert("削除しました。");

        }
      	  else alert("削除を取消ししました。");
      	}
